---
- name: Unir nodo Worker al clúster K3s
  ansible.builtin.shell: |
    curl -sfL https://get.k3s.io | K3S_URL=https://{{ k3s_control_plane_ip }}:6443 K3S_TOKEN="{{ hostvars['svr-airflow']['cluster_token'] }}" sh -
  args:
    creates: /usr/local/bin/k3s-agent